---
import { fixedFooterPages } from "../core/fixed-pages";
import { siteConfig } from "../core/site-data";

const year = new Date().getUTCFullYear();
---

<footer class="site-footer">
  <div class="shell site-footer__grid">
    <div>
      <span class="eyebrow">Player index</span>
      <p class="site-footer__statement">
        Guides, answers, and game reference for the next session.
      </p>
    </div>
    <div class="site-footer__meta">
      <p>{siteConfig.brand.name} / {year}</p>
      <p>{siteConfig.brand.tagline}</p>
      <nav class="site-footer__links" aria-label="Company and legal">
        {fixedFooterPages.map((page) => <a href={page.route}>{page.pageId}</a>)}
      </nav>
      <a href="/#top">Back to top</a>
    </div>
  </div>
</footer>
